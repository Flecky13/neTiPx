<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="neTiPx.Views.ToolsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:neTiPx.ViewModels"
    mc:Ignorable="d">
    <Page.DataContext>
        <viewModels:ToolsViewModel />
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header mit Image -->
        <Border Grid.Row="0" Style="{StaticResource CardBorder}" Padding="24" Margin="24,24,24,0">
            <Grid ColumnDefinitions="*,Auto">
                <StackPanel Spacing="8">
                    <TextBlock Text="Tools" Style="{StaticResource SectionTitle}" FontSize="24" FontWeight="Bold" />
                    <TextBlock Text="Netzwerk-Utilities und Diagnose-Tools" />
                </StackPanel>
                <Image
                    Grid.Column="1"
                    Source="ms-appx:///Assets/toolicon.png"
                    Width="120"
                    Height="120"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right" />
            </Grid>
        </Border>

        <!-- Einklappbares Menü mit Unterseiten -->
        <NavigationView
            x:Name="ToolsNavView"
            Grid.Row="1"
            PaneDisplayMode="Left"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            OpenPaneLength="200"
            CompactPaneLength="48"
            IsPaneToggleButtonVisible="True"
            Margin="24,16,24,24"
            SelectionChanged="ToolsNavView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="PING" Tag="Ping">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE909;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="WLAN" Tag="Wlan">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE701;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Platzhalter" Tag="Placeholder">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8B8;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.Content>
                <ScrollViewer>
                    <Grid>
                        <!-- PING Unterseite -->
                        <StackPanel x:Name="PingPanel" Spacing="16" Padding="24" Visibility="Collapsed">
                            <TextBlock Text="PING Tool" Style="{StaticResource SectionTitle}" FontSize="20" FontWeight="SemiBold" />

                            <!-- Konfiguration -->
                            <Border Style="{StaticResource CardBorder}" Padding="16">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Konfiguration" FontWeight="SemiBold" />

                                    <Grid ColumnDefinitions="*,120,Auto" ColumnSpacing="8">
                                        <TextBox
                                            x:Name="NewPingTargetTextBox"
                                            Grid.Column="0"
                                            Header="Zieladresse"
                                            PlaceholderText="IP-Adresse oder Hostname (z.B. 8.8.8.8, google.com)"
                                            VerticalAlignment="Center" />
                                        <NumberBox
                                            x:Name="PingIntervalNumberBox"
                                            Grid.Column="1"
                                            Header="Sekunden"
                                            Value="5"
                                            Minimum="1"
                                            Maximum="60"
                                            SpinButtonPlacementMode="Compact"
                                            VerticalAlignment="Center" />
                                        <Button
                                            Grid.Column="2"
                                            Content="Hinzufügen"
                                            Click="AddPingTarget_Click"
                                            Style="{StaticResource AccentButtonStyle}"
                                            VerticalAlignment="Bottom" />
                                    </Grid>
                                </StackPanel>
                            </Border>

                            <!-- Ping-Ziele Liste -->
                            <Border Style="{StaticResource CardBorder}" Padding="16">
                                <StackPanel Spacing="12">
                                    <!-- Überschrift mit Spalten-Labels -->
                                    <Grid ColumnDefinitions="*,100,30,100,30,Auto" ColumnSpacing="12">
                                        <TextBlock Text="Ping-Ziele" FontWeight="SemiBold" Grid.Column="0" />
                                        <TextBlock Text="IPv4" FontWeight="SemiBold" Grid.Column="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
                                        <TextBlock Text="" Grid.Column="2" />
                                        <TextBlock Text="IPv6" FontWeight="SemiBold" Grid.Column="3" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="12" />
                                        <TextBlock Text="" Grid.Column="4" />
                                        <TextBlock Text="" Grid.Column="5" />
                                    </Grid>

                                    <ItemsControl x:Name="PingTargetsItemsControl" ItemsSource="{Binding PingTargets}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border
                                                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="4"
                                                    Padding="12"
                                                    Margin="0,0,0,8">
                                                    <Grid RowDefinitions="Auto,Auto" ColumnDefinitions="*,100,30,100,30,Auto" ColumnSpacing="12" RowSpacing="8">
                                                        <!-- Zeile 1: Aktuelle Daten -->

                                                        <!-- Adresse/IP -->
                                                        <TextBox
                                                            Grid.Row="0"
                                                            Grid.Column="0"
                                                            Text="{Binding Target, Mode=OneWay}"
                                                            IsReadOnly="True"
                                                            VerticalAlignment="Center" />

                                                        <!-- Antwortzeit IPv4 -->
                                                        <TextBox
                                                            Grid.Row="0"
                                                            Grid.Column="1"
                                                            Text="{Binding ResponseTimeIpv4, Mode=OneWay}"
                                                            IsReadOnly="True"
                                                            PlaceholderText="-- ms"
                                                            VerticalAlignment="Center"
                                                            Visibility="{Binding ShowIPv4}" />

                                                        <!-- Ampel IPv4 -->
                                                        <Ellipse
                                                            Grid.Row="0"
                                                            Grid.Column="2"
                                                            Width="12"
                                                            Height="12"
                                                            Fill="{Binding StatusColorIpv4}"
                                                            VerticalAlignment="Center"
                                                            HorizontalAlignment="Center"
                                                            ToolTipService.ToolTip="IPv4 Status"
                                                            Visibility="{Binding ShowIPv4}" />

                                                        <!-- Antwortzeit IPv6 -->
                                                        <TextBox
                                                            Grid.Row="0"
                                                            Grid.Column="3"
                                                            Text="{Binding ResponseTimeIpv6, Mode=OneWay}"
                                                            IsReadOnly="True"
                                                            PlaceholderText="-- ms"
                                                            VerticalAlignment="Center"
                                                            Visibility="{Binding ShowIPv6}" />

                                                        <!-- Ampel IPv6 -->
                                                        <Ellipse
                                                            Grid.Row="0"
                                                            Grid.Column="4"
                                                            Width="12"
                                                            Height="12"
                                                            Fill="{Binding StatusColorIpv6}"
                                                            VerticalAlignment="Center"
                                                            HorizontalAlignment="Center"
                                                            ToolTipService.ToolTip="IPv6 Status"
                                                            Visibility="{Binding ShowIPv6}" />

                                                        <!-- Löschen Button -->
                                                        <Button
                                                            Grid.Row="0"
                                                            Grid.Column="5"
                                                            Click="DeletePingTarget_Click"
                                                            Tag="{Binding}"
                                                            ToolTipService.ToolTip="Ziel löschen"
                                                            VerticalAlignment="Center"
                                                            Padding="8"
                                                            MinWidth="0">
                                                            <FontIcon Glyph="&#xE74D;" FontSize="16" />
                                                        </Button>

                                                        <!-- Zeile 2: Statistiken -->

                                                        <!-- Statistiken gesamt wenn beide sichtbar, sonst nur eine -->
                                                        <StackPanel
                                                            Grid.Row="1"
                                                            Grid.Column="0"
                                                            Grid.ColumnSpan="6"
                                                            Orientation="Horizontal"
                                                            Spacing="24"
                                                            Margin="8,0,0,0">
                                                            <TextBlock
                                                                Text="{Binding StatisticsIpv4}"
                                                                FontSize="13"
                                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                VerticalAlignment="Center"
                                                                Visibility="{Binding ShowIPv4}" />
                                                            <TextBlock
                                                                Text="{Binding StatisticsIpv6}"
                                                                FontSize="13"
                                                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                VerticalAlignment="Center"
                                                                Visibility="{Binding ShowIPv6}" />
                                                        </StackPanel>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </StackPanel>

                        <!-- WLAN Unterseite -->
                        <StackPanel x:Name="WlanPanel" Spacing="16" Padding="24" Visibility="Collapsed">
                            <TextBlock Text="WLAN Scanner" Style="{StaticResource SectionTitle}" FontSize="20" FontWeight="SemiBold" />
                            <!-- Hier kommt später der WLAN-Inhalt -->
                        </StackPanel>

                        <!-- Platzhalter Unterseite -->
                        <StackPanel x:Name="PlaceholderPanel" Spacing="16" Padding="24" Visibility="Collapsed">
                            <TextBlock Text="Platzhalter" Style="{StaticResource SectionTitle}" FontSize="20" FontWeight="SemiBold" />
                            <!-- Hier kommt später weiterer Inhalt -->
                        </StackPanel>
                    </Grid>
                </ScrollViewer>
            </NavigationView.Content>
        </NavigationView>
    </Grid>
</Page>
