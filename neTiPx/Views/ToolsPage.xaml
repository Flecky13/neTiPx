<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Name="ToolsPageRoot"
    x:Class="neTiPx.Views.ToolsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewModels="using:neTiPx.ViewModels"
    mc:Ignorable="d">
    <Page.DataContext>
        <viewModels:ToolsViewModel />
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header mit Image -->
        <Border Grid.Row="0" Style="{StaticResource CardBorder}" Padding="24" Margin="24,24,24,0">
            <Grid ColumnDefinitions="*,Auto">
                <StackPanel Spacing="8">
                    <TextBlock Text="Tools" Style="{StaticResource SectionTitle}" FontSize="24" FontWeight="Bold" />
                    <TextBlock Text="Netzwerk-Utilities und Diagnose-Tools" />
                </StackPanel>
                <Image
                    Grid.Column="1"
                    Source="ms-appx:///Assets/toolicon.png"
                    Width="120"
                    Height="120"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right" />
            </Grid>
        </Border>

        <!-- Einklappbares Menü mit Unterseiten -->
        <NavigationView
            x:Name="ToolsNavView"
            Grid.Row="1"
            PaneDisplayMode="Left"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            OpenPaneLength="200"
            CompactPaneLength="48"
            IsPaneToggleButtonVisible="True"
            Margin="24,16,24,24"
            SelectionChanged="ToolsNavView_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="PING" Tag="Ping">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE909;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="WLAN" Tag="Wlan">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE701;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Netzwerk-Rechner" Tag="NetworkCalculator">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8BD;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Netzwerkscanner" Tag="NetworkScanner">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE774;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.Content>
                <ScrollViewer>
                    <Grid>
                        <!-- PING Unterseite -->
                        <Frame x:Name="PingPanel" Visibility="Collapsed" />

                        <!-- WLAN Unterseite -->
                        <Frame x:Name="WlanPanel" Visibility="Collapsed" />

                        <!-- Netzwerk-Rechner Unterseite -->
                        <Frame x:Name="NetworkCalculatorPanel" Visibility="Collapsed" />

                        <!-- Netzwerkscanner Unterseite -->
                        <Frame x:Name="NetworkScannerPanel" Padding="24" Visibility="Collapsed" />
                    </Grid>
                </ScrollViewer>
            </NavigationView.Content>
        </NavigationView>
    </Grid>
</Page>
